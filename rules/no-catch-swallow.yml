# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: no-catch-swallow
message: "Do not silently swallow errors with `.catch(() => undefined)`. Log, handle, or propagate the error."
severity: error
language: TypeScript
rule:
  pattern: $PROMISE.catch(() => undefined)
