# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: no-as-object
message: "Disallow asObject helper"
severity: error
language: TypeScript
rule:
  pattern: asObject($$$ARGS)