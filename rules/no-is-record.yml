# yaml-language-server: $schema=https://raw.githubusercontent.com/ast-grep/ast-grep/main/schemas/rule.json

id: no-is-record
message: "Disallow isRecord helper"
severity: error
language: TypeScript
rule:
  pattern: isRecord($$$ARGS)