/**
 * Groups surface type re-exports: exposes persisted UI group records
 * for consumers without depending on internal controller paths.
 */
export type { PersistedUiGroup } from "./controllers/persistence";
